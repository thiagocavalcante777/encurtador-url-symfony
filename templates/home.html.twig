{% include 'parts/header.html.twig' %}
{% block body %}
    <div id="container-gerar-url" class="container-fluid">
        <form id="form-gerar-url" action="{{ path('gerar_url') }}" method="POST">
            <div class="row">
                <div class="col">
                    <input id="input-url" name="url_original" placeholder="url original" class="form-control">
                </div>
                <div class="col">
                    <button type="submit" class="btn btn-primary mb-2">Encurtar Url</button>
                </div>
            </div>
        </form>
    </div>
{#    <div data-controller="home"></div>#}
{#    <div {{ stimulus_controller('home') }}></div>#}
{#    <div data-controller="clipboard" data-clipboard-success-content-value="Copied!">#}
{#        <input type="text" value="Click the button to copy me!" data-clipboard-target="source" />#}
{#        <button type="button" data-action="clipboard#copy" data-clipboard-target="button">Copy to clipboard</button>#}
{#    </div>#}
{% endblock %}
{% include 'parts/footer.html.twig' %}
